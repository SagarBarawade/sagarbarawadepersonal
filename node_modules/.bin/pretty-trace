#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pretty-trace/bin/pretty-trace" "$@"
  ret=$?
else 
  node  "$basedir/../pretty-trace/bin/pretty-trace" "$@"
  ret=$?
fi
exit $ret
